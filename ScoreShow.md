# 수행평가 점수 디스플레이 웹앱 설계서 (오프라인 전용 PWA)

## 📘 프로젝트 이름
이 프로그램은 교사가 학생의 수행평가 점수를 안전하게, 한 명씩 개별적으로 보여주는 도구입니다.  
수행평가 출력물을 뽑아 다른 학생 정보를 가려가며 점수를 알려주던 번거로운 과정을 해결하기 위해 만들어졌습니다.  
프로젝트 이름: **ScoreShow**

> **선택 이유:** "Show"는 점수를 "보여주다"는 핵심 기능을 명확히 전달하며, 짧고 기억하기 쉬워 교사 커뮤니티에서 직관적으로 이해할 수 있습니다.

---

## 1. 개요
교사용 수행평가 점수 확인 도구를 웹앱 형태로 구현한다.  
학생을 한 명씩 개별적으로 보여주어 비밀을 보호하면서 점수를 확인할 수 있도록 한다.  
과거 수행평가 출력물을 뽑아 다른 학생 정보를 가려가며 점수를 알려주던 번거로운 과정을 해결한다.  
주요 목표는 USB·로컬 배포의 한계를 벗어나되, **서버 통신 없이 100% 오프라인 동작**하는 것이다.  
데이터는 **브라우저 내 로컬 저장소(IndexedDB)** 에만 머무르며, 인터넷이 없어도 사용 가능해야 한다.

---

## 2. 사용자 시나리오
1. 교사가 사이트를 브라우저에서 실행하거나 PWA로 설치한다.  
2. 엑셀(.xlsx/.csv) 파일을 업로드한다.  
3. 자동으로 학년/반/번호/이름/평가영역/점수/만점 컬럼을 매핑한다.  
4. 목록에서 학생을 선택하면 해당 학생의 점수를 표시한다.  
5. 발표 모드를 시작하여 학생에게 점수를 보여준다.  
   - 화살표 키(←/→/↑/↓)로 다음/이전 학생으로 이동
   - 한 명씩 개별적으로 표시되어 비밀 보호
   - 큰 폰트로 표시되어 정보를 확실하게 파악 가능
   - 출력물을 가려가며 보여주던 번거로운 과정 없이 편리하게 사용
6. 종료 시 로컬 저장 데이터는 자동 파기(또는 수동 삭제).  

---

## 3. 기술 스택
- **React + Vite + TypeScript**
- **Tailwind CSS** : 간결한 UI 구성
- **SheetJS (xlsx)** : 엑셀 파일 파싱
- **Zustand** : 상태 관리
- **idb-keyval** : IndexedDB 로컬 저장소 관리
- **crypto-js (AES)** : 데이터 암호화(선택)
- **vite-plugin-pwa** : PWA 설치/오프라인 실행 지원

---

## 4. 시스템 아키텍처
```
사용자
  ↓
브라우저 (PWA)
  ├─ React UI
  │   ├─ FilePicker (엑셀 업로드)
  │   ├─ MappingWizard (컬럼 매핑)
  │   ├─ StudentList (목록/검색)
  │   ├─ PreviewPanel (학생 정보 미리보기)
  │   └─ Presenter (발표 모드)
  │
  ├─ Core Logic
  │   ├─ parseExcel.worker.ts (Web Worker로 엑셀 파싱)
  │   ├─ mapping.ts (정규식 기반 자동 매핑)
  │   ├─ normalize.ts (데이터 정규화)
  │   ├─ storage.ts (IndexedDB 저장 및 암호화)
  │   └─ no-network.ts (네트워크 요청 차단)
  │
  └─ IndexedDB (로컬 데이터 캐시)
```

---

## 5. 주요 기능 정의

### 5.1 엑셀 파서
- SheetJS 라이브러리를 이용해 `.xlsx` / `.csv` 지원.  
- 헤더 추론(1~5행 스캔) 후 정규식 매핑:
  - 학년: `학년|grade`
  - 반: `반|class|학급`
  - 번호: `번호|번|student_no`
  - 이름: `이름|성명|name`
  - 평가영역: `평가.?영역|과제명|항목`
  - 점수: `점수|득점|원점수|성취점`
  - 만점: `만점|배점|총점`
- 병합셀 및 수식은 값으로 변환 후 파싱.

### 5.2 데이터 정규화
- “10점”, “10/10”, “10 / 10” → 숫자형 분리.  
- 숫자 외 입력, 음수, 만점 초과 시 경고 표시.  
- 한글 이름, 공백, 전각 문자 제거.

### 5.3 발표 모드(UI)
- 학생을 한 명씩 개별적으로 표시 (큰 폰트 사용):
  ```
  2학년 5반 12번 홍길동
  [영어 말하기 평가]
  8 / 10
  ```
- 화이트아웃 기능:
  - 화살표 키를 한 번 누르면 화면이 비워짐 (화이트아웃 상태)
  - 같은 방향 키를 다시 누르면 다음/이전 학생으로 이동
  - 반대 방향 키를 누르면 화이트아웃만 해제되고 학생 이동 없음
  - 학생 전환 전 짧은 대기 시간을 제공하여 자연스러운 전환 효과
  - 학생 리스트에서 현재 전환 위치를 녹색 마커로 표시하여 어떤 학생 사이를 전환하는지 명확히 표시
- 전환 효과: 페이드/블라인드(300~500ms).  
- 단축키:
  - ←/→/↑/↓ : 학생 이동 (화이트아웃 포함)
  - F : 전체화면
  - Esc : 발표모드 종료
- 비밀 보호: 한 번에 한 학생만 표시하여 다른 학생의 점수가 보이지 않도록 함

### 5.4 데이터 저장 정책
| 모드 | 설명 |
|------|------|
| 세션 전용 | 새로고침 시 데이터 삭제 (기본값) |
| 로컬 보존 | IndexedDB에 AES 암호화 저장 |
| 내보내기/가져오기 | 암호화 JSON으로 백업 가능 |

### 5.5 PWA/오프라인
- Service Worker로 정적 자원 캐시(App Shell).  
- 첫 방문 후 오프라인에서도 동작.  
- “앱 설치” 버튼으로 바탕화면/작업표시줄 등록 가능.

### 5.6 네트워크 차단
- `no-network.ts` 유틸에서 `fetch`, `XMLHttpRequest`, `WebSocket` 오버라이드.  
- CSP:  
  ```
  default-src 'self';
  connect-src 'none';
  img-src 'self' blob:;
  font-src 'self';
  ```

---

## 6. 화면 설계 요약

### 6.1 메인화면
| 구역 | 내용 |
|------|------|
| 좌측 | 학생 목록(검색 포함) |
| 우측 | 선택된 학생 미리보기 |
| 상단 | 엑셀 업로드, 설정 버튼 |
| 하단 | 발표모드 진입 버튼 |

### 6.2 발표모드 화면
| 요소 | 설명 |
|------|------|
| 중앙 | 학년/반/번호/이름, 평가영역, 점수 (큰 폰트로 명확하게 표시) |
| 단축키 | ←/→/↑/↓/F/Esc |
| 전환 | 페이드 효과 |
| 목적 | 학생을 한 명씩 개별적으로 보여주어 비밀 보호, 출력물을 가려가며 보여주던 번거로운 과정 해결 |

---

## 7. 개발 및 빌드
### 로컬 실행
```bash
npm install
npm run dev
```
→ http://localhost:5173

### 빌드
```bash
npm run build
npx serve dist
```
→ http://localhost:8080 (완전 오프라인 실행 가능)

---

## 8. 보안 및 법적 대응
- 데이터는 로컬에만 저장되어 **개인정보 처리 행위로 간주되지 않음.**  
- 외부 전송 없음, 클라우드 동기화 폴더 사용 금지.  
- “데이터 파기” 버튼으로 모든 캐시 삭제.

---

## 9. 성공 기준
| 항목 | 기준 |
|------|------|
| 엑셀 자동 인식 | 2종 이상 포맷 정상 파싱 |
| 발표 모드 | 한 학생만 표시, 부드러운 전환 |
| 오프라인 동작 | 인터넷 끊겨도 완전 작동 |
| 데이터 파기 | IndexedDB 완전 삭제 |
| 외부 통신 | Network 요청 0 |

---

## 10. 향후 확장
- 다중 평가영역 탭 추가  
- 점수 이력 비교 기능  
- UI 테마(밝은/어두운 모드)  
- 매핑 규칙 커스터마이징 JSON 내보내기
